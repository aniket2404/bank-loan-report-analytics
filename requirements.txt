# Core analysis
pandas>=2.0,<3.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
squarify>=0.4.3          # tree map for the Home Ownership visual
openpyxl>=3.1            # read/write .xlsx sources
python-dotenv>=1.0       # load .env for database settings

# Notebook
jupyter>=1.0
notebook>=7.0

# Optional: connect Python directly to SQL Server instead of reading the CSV
sqlalchemy>=2.0
pyodbc>=5.0

# Development
pytest>=7.4
